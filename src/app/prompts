You are an expert Next.js (App Router), TypeScript, and Tailwind CSS developer.
We are building a highly interactive, premium developer portfolio.

Task 1: Setup the core architecture, global theming, and file structure.

1. Create a strict, clean folder structure:
   - `src/app/` (Next.js routing)
   - `src/components/ui/` (shadcn/ui and reusable base components)
   - `src/components/sections/` (Major page sections)
   - `src/components/animations/` (Framer motion wrappers and micro-interactions)
   - `src/lib/` (Utility functions like `cn` for tailwind-merge)

2. Implement Global Theming:
   - Install and configure `next-themes` for a flawless dark mode.
   - Set the default theme to dark. The background should be a deep OLED black (`bg-black` or `#000000`), with high-contrast, subtle borders (using zinc-900).
   - Ensure CSS transitions on theme changes are smooth.

3. Create the global `RootLayout` in `src/app/layout.tsx`. Include a minimalist, transparent top navigation bar and wrap the children in a Framer Motion `AnimatePresence` provider for future transitions.

Provide the bash commands to install necessary dependencies (Framer motion, next-themes, tailwind-merge, clsx) and generate the layout, global.css, and tailwind config files.

Task 2: Build the global interactive layers for the portfolio.

1. Custom Cursor (`src/components/animations/CustomCursor.tsx`):
   - Build a fluid custom cursor using Framer Motion (`useScroll`, `useSpring`).
   - It should track the mouse smoothly with slight spring physics (stiffness: 100, damping: 20).
   - Use `mix-blend-difference` so it elegantly inverts colors when hovering over text or images. Disable it gracefully on mobile viewports.

2. Pinned Animated Socials Dock (`src/components/ui/FloatingDock.tsx`):
   - Create a fixed floating dock pinned to the bottom center of the screen.
   - Include icons for GitHub, LinkedIn, and Email.
   - Use Framer Motion so when a user hovers over an icon, it smoothly scales up (magnifies) and nudges upward, mimicking a premium OS application dock.

3. Command Palette (`src/components/ui/CommandPalette.tsx`):
   - Implement a shadcn/ui `<Command>` modal triggered by `Cmd+K` or `Ctrl+K`.
   - Include options to: Toggle Dark Mode, Jump to Projects, Jump to Education, and Download Resume.

Integrate these components into the `RootLayout` so they persist across the entire site. Provide the code for these three components.

Task 3: Build the main scrollable page sections inside `src/components/sections/` and render them in `src/app/page.tsx`.

1. Hero Section (`Hero.tsx`):
   - Use a minimalist Aurora or Spotlight background effect.
   - Include a bold, clean headline.
   - Add a magnetic Framer Motion button saying "View My Work" that smooth-scrolls to the Projects section.

2. About Me Narrative (`About.tsx`):
   - Implement a "Text Reveal" scroll animation where sentences fade in line-by-line as the user scrolls.
   - Write placeholder copy highlighting a background in UI/UX design, an affinity for creating clean, integrated digital interfaces, and a non-linear approach to complex problem-solving.

3. Education Timeline (`Education.tsx`):
   - Build a vertical scroll-linked timeline using Framer Motion. A SVG line should draw downwards as the user scrolls.
   - Populate the timeline with a focus on a Bachelor of Engineering at McMaster University.
   - Break down rigorous STEM foundations: note coursework in multivariable calculus, electrostatics, and mechanics to establish technical depth despite a lack of formal work experience.

4. Bento Grid Skills (`BentoSkills.tsx`):
   - Create an Apple-style Bento Grid layout for technical skills.
   - Dedicate one large grid cell to an infinitely scrolling Marquee or interactive 3D Icon Cloud featuring: Next.js, Tailwind, TypeScript, React, and Framer Motion.

Provide the code for these sections and update `page.tsx` to stack them.

Task 4: Build the Project Showcase with Sneak Peeks and Shared Element Transitions.

1. Project Sneak Peek Cards (`src/components/sections/Projects.tsx`):
   - Create a grid of project cards. As the user scrolls them into view, they should tilt dynamically (3D Card Effect).
   - Inside the card, wrap a video or GIF of the project inside a simulated Mac Safari browser window component.
   - Add shadcn/ui badges for the tech stack used.

2. Dynamic Routing & View Transitions:
   - Create a dynamic route `src/app/projects/[slug]/page.tsx` for dedicated project case studies.
   - Implement a seamless transition from the home page to the project page.
   - _Crucial Context:_ Because we are using the Next.js App Router, utilize Next.js 15's native `<ViewTransition>` API (or Framer Motion's `layoutId` within a single-page expandable modal overlay if View Transitions are unavailable in the current setup) so the project card fluidly expands to become the hero section of the new page without a hard reload.

3. Project Details Page:
   - The expanded page should feature a sticky "Back to Portfolio" top-left button, the expanded browser mockup, and clean markdown-styled typography sections for "The Problem", "Architecture", and "Links".

Provide the code for the Projects showcase and the dynamic slug page. Ensure the transition logic is heavily commented.
